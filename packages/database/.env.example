# Local development (Docker): postgresql://deeply:deeply@localhost:5432/deeply
# Production (Neon pooled):   postgresql://<user>:<pass>@<host>-pooler.<region>.neon.tech/<db>?sslmode=require
DATABASE_URL=""

# Same as DATABASE_URL for local Docker.
# Production (Neon direct — non-pooled, required for migrations):
# postgresql://<user>:<pass>@<host>.<region>.neon.tech/<db>?sslmode=require
DIRECT_URL=""
